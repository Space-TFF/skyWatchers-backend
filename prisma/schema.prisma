// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// schema.prisma
model Event {
  id          Int     @id @default(autoincrement())
  name        String
  description String
  address     String
  lat         Float
  lng         Float
  isPublic    Boolean

  // Define a many-to-one relationship with User
  // createdBy   User     @relation(fields: [email], references: [email])
}

// schema.prisma
// model User {
//   id       Int      @id @default(autoincrement())
//   email    String   @unique

//   // Define a one-to-many relationship with Event
//   events   Event[]
// }
